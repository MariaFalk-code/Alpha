@* Basecode for modal from Material Tailwind, modified to fit the design of the project. *@
<div data-dialog-backdrop="sign-in-dialog"
     data-dialog-backdrop-close="true"
     class="pointer-events-none fixed inset-0 z-[999] grid h-screen w-screen place-items-center bg-black bg-opacity-60 opacity-0 backdrop-blur-sm transition-opacity duration-300">
    <div data-dialog="sign-in-dialog"
         class="relative mx-auto w-full max-w-[37.5rem] rounded-lg overflow-hidden shadow-md transition-all duration-300 opacity-0 -translate-y-14">

        <div class="relative flex flex-col bg-white max-h-[90dvh] overflow-y-auto">
            <div class="relative mt-12 items-center flex justify-center">
                <h1 class="text-3xl font-bold">
                    Login
                </h1>
            </div>

            <form method="post" asp-controller="Auth" asp-action="Login" novalidate class="flex flex-col gap-4 py-8 px-24">

                <div class="w-full min-w-[200px]">
                    <label class="block mb-2 text-sm font-semibold">
                        Email
                    </label>
                    <input type="email" class="text-input" placeholder="Your email address" />
                </div>

                <div class="w-full min-w-[200px] mb-2">
                    <label class="block mb-2 text-sm font-semibold">
                        Password
                    </label>
                    <input type="password" class="text-input" placeholder="Enter your password" />
                </div>

                @await Html.PartialAsync("_Checkbox", ("rememberMe", "Remember Me", false))

                <button class="btn-primary mt-6" type="button">
                    Log In
                </button>
            </form>

            <div class="py-6 px-24 pt-0">
                <p class="flex justify-center mt-20 text-sm">
                    Don&apos;t have an account?
                    <a href="#"
                       data-close="sign-in-dialog"
                       data-switch-dialog="register-dialog"
                       class="ml-1 text-sm font-semibold text-blue-accent">
                        Sign Up
                    </a>

                </p>
            </div>

        </div>
    </div>
</div>
